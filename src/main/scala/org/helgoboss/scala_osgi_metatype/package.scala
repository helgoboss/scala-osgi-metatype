package org.helgoboss

/**
 * Scala interfaces, adapters and builders for easily building OSGi metatype descriptions.
 *
 * I created totally new Scala interfaces plus adapters instead of directly implementing the Java interfaces
 * of the OSGi Metatype API in order to prevent name clashes and confusion about what method to call.
 * Scala Swing does it the same way, I guess for the same reasons.
 */
package object scala_osgi_metatype {

}
